import {StyleSheet} from 'react-native';
import {SECONDARY_BACKGROUND, SECONDARY_TEXT} from '../../constants/colors';

export default StyleSheet.create({
  container: {height: '100%', backgroundColor: SECONDARY_BACKGROUND},
  footerText: {color: SECONDARY_TEXT, fontSize: 14, textAlign: 'center'},
});
